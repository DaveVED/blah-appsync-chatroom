{
    "name": "appsync-chatroom",
    "private": true,
    "workspaces": [
        "packages/*"
    ],
    "scripts": {
        "format": "prettier --write \"**/*.{ts,tsx,md}\""
    },
    "devDependencies": {
        "prettier": "^3.2.5"
    },
    "engines": {
        "node": ">=18"
    },
    "dependencies": {
        "bcrypt": "^5.1.1"
    }
}